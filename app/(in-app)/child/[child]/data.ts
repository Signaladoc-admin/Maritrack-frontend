import { IChildProfile } from "@/features/onboarding/personal/types";

// Dummy profiles removed. Child data is fetched directly via useGetChild and useParentChildren.
export const childrenProfiles: IChildProfile[] = [];
